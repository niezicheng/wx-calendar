<view class="intro">日历组件</view>
  <calendar
    multi-select="{{multiSelect}}"
    current="{{current}}"
    weeks-type="{{weeksType}}"
    min-Date="{{minDate}}"
    max-Date="{{maxDate}}"
    date-format="{{dateFormat}}"
    bind:select="select"
    bind:today="today"
  />