<view class="intro">日历组件</view>
  <calendar
    multi-select="{{multiSelect}}"
    current="{{current}}"
    min-Date="{{minDate}}"
    max-Date="{{maxDate}}"
    date-format="{{dateFormat}}"
    bind:select="select"
    bind:today="today"
  />